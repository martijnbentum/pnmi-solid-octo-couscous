[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "pnmi"
version = "0.1.7"
description = "Utilities for computing PNMI and related clustering metrics"
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["numpy"]
